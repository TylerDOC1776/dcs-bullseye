# Configs

Example configuration files for all packages. Copy and rename (removing `.example`) for local use. Do not commit real tokens or secrets.

| File | Description |
|------|-------------|
| `orchestrator.example.json` | Orchestrator config — API key, host, port, DB path |
| `agent.example.json` | Agent config — DCS instance paths, NSSM, orchestrator URL |
